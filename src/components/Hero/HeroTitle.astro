---
import type { HeroTitleProps } from './Hero.types';

const { name } = Astro.props as HeroTitleProps;
---

<div class="animate-fadeIn text-4 md:text-8 lg:text-xl font-bold tracking-[-0.075rem] md:tracking-[-0.12781rem] lg:tracking-[-2.5px] leading-none mb-5 md:mb-[3.75rem] lg:mb-[2.7rem] md:max-w-[70%] relative text-balance">
  <div class="">
    <span>Nice to</span>{' '}
    <span class="md:whitespace-nowrap">
      meet you! I'm
    </span>{' '}
    <h1 class="inline-block relative after:content-[''] after:block after:absolute after:bottom-0 after:left-0 after:bg-red-500 after:w-full after:h-1 after:md:h-1.5 after:transition-all after:duration-300 hover:after:scale-y-[2]">{name}</h1>.
  </div>
</div>
